import { EditorRequisitos } from '../../app/components/acciones/requisitos/EditorRequisitos';
export default function RequisitosPage({ project }: { project?: any }) {
  return <EditorRequisitos project={project} />;
}
